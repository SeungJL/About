import { atom } from "recoil";

export const isGatherEditState = atom({
  key: "isGatherEdit",
  default: false,
});
export const isGroupEditState = atom({
  key: "isGroupEdit",
  default: false,
});
