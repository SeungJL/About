import styled from "styled-components";
import { ModalXXL } from "../styles/layout/modal";

function CheckingServerModal() {
  return <Layout>현재 서버</Layout>;
}

const Layout = styled(ModalXXL)``;

export default CheckingServerModal;
