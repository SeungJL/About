import styled from "styled-components";
import { ModalXs } from "../../../styles/LayoutStyles";

function CancelVoteModal() {
  return <Layout></Layout>;
}

const Layout = styled(ModalXs)``;

export default CancelVoteModal;
